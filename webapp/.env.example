# EMTUSA credentials now live server-side in backend/.env
# This file is kept for reference — the frontend no longer needs auth vars.
# VITE_* vars have been removed: the backend proxy handles EMTUSA authentication.
